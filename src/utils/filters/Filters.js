import {
    extractLGAData,
    extractDisabilityData,
    extractEducationData,
    extractGenderData,
    extractMaritalStatusData
} from "../Gridd/Extraction"

export const genderFilter = (testArr, dataArr) => {
    let genderResult = []
    testArr.map(res => {
        if(res === "female"){
            
        }
    })
}

export const profileFilter = (testArr, dataArr) => {
    let profileResult = []

}

export const lgaFilter = (testArr, dataArr) => {
    let lgaResult = []

}

export const educationFilter = (testArr, dataArr) => {
    let educationResult = []

}

export const maritalstatusFilter = (testArr, dataArr) => {
    let maritalResult = []

}