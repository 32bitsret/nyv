import {
    GET_NOTIFICATIONS, 
    GET_NOTIFICATION_ERROR
} from "../Constants"
import {} from "../../api/apiURL"
import axios from "axios"

export const getNotification = () => dispatch => {
    axios({
        method: "",
        url: "",
        data: ""
    })
    .then(res => {

    })
    .catch(err => {

    })
}